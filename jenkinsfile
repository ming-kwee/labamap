pipeline {
  agent any
  stages {
    stage("verify tooling") {
      steps {
        sh '''
          sudo groupadd docker
          sudo usermod -aG docker $USER
          docker version
          docker info
          docker compose version
          curl --version
          jq --version
        '''
      }    
    }  
  }
}
