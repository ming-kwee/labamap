pipeline {
  // Assign to docker agent(s) label, could also be 'any'
  
  agent {
    docker { image 'node:16.13.1-alpine' }
  }

    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
} 
